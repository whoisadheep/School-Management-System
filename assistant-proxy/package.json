{
  "name": "assistant-proxy",
  "version": "0.1.0",
  "description": "Proxy server for AI assistant which holds the Gemini API key and enforces per-installation rate limits.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "axios": "^1.5.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "helmet": "^7.0.0"
  }
}
